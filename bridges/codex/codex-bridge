#!/usr/bin/env bash
set -euo pipefail
if [[ "${CODEX_BRIDGE_DRY_RUN:-0}" == "1" ]]; then
  echo "codex bridge dry-run ok"
  exit 0
fi
echo "error: live Codex bridge is not bundled in v0.1" >&2
exit 124
