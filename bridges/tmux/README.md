# tmux Bridge Dependency

Live tmux mode requires a `tmux-bridge` compatible command supplied by the user or a future vendored adapter with compatible licensing.

v0.1 dry-run mode works without tmux or tmux-bridge.
