# Memory Model

Memory is optional. Public default uses local files only.

```text
memory search <query>
memory add <key> <content>
memory list
memory export <path>
```

Network memory providers belong in private overlays.
