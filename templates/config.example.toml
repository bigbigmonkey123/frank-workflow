[overlay]
enabled = false
dir = ""

[adapters.developer]
command = "codex-bridge"

[adapters.reviewer]
command = "claude-official-bridge"

[adapters.scout]
command = "gemini-bridge"

# Illustrative overlay policy; the public bootstrap does not enforce these keys.
[workflow]
design_review_rounds = 2
final_review_rounds = 2
heavy_implementation_rounds = 3

[capacity]
fallback_bins = []
idempotent = false
